============
Installation
============

At the command line::

    $ easy_install django-dynamic-views

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-dynamic-views
    $ pip install django-dynamic-views
